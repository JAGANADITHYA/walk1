import { DynamicBackground } from "./DynamicBackground";

// Legacy component - now just wraps DynamicBackground for compatibility
export function RainbowBackground() {
  return <DynamicBackground />;
}
